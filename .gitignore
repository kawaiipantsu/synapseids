# Binaries
/synapsed
/synapse
/synapse-sensor
*.test
*.out

# Build / release output
/dist/
/build/

# Web UI (Node). The built bundle web/dist/ IS committed and embedded by
# web/web.go; only the transient toolchain state is ignored.
/web/ui/node_modules/
/web/ui/.vite/
/web/ui/*.tsbuildinfo

# Coverage
coverage.out
coverage.html

# Runtime data
/data/
*.db
*.db-journal
*.db-wal

# Go workspace
go.work
go.work.sum

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~
